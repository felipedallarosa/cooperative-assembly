CREATE TABLE AGENDA
(
    ID NUMBER(10,0) NOT NULL ENABLE,
    DESCRIPTION VARCHAR2(300) NOT NULL,

    CONSTRAINT "AGENDA_PK" PRIMARY KEY ("ID") ENABLE,
    CONSTRAINT "UK_AGENDA_DESCRIPTION" UNIQUE (DESCRIPTION)
);